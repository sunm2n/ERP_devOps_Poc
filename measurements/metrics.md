# 지표 기록표

`plan.md` 5장. **측정한 자리에서 채운다.** 미측정은 `미측정` 과 이유를 적는다.

`근거` 칸에는 값을 뽑은 명령이나 파일을 적는다 — 예: `raw/h3-window-a.txt / 2026-09-03 김OO(절차 설계자)`.

> **추적 파일에는 식별자를 쓰지 않는다.** 주소·호스트명·FQDN 은 `<내부IP-A>`, `<레지스트리-A>`
> 같은 자리표시자로 적고, 실값 대응표는 `raw/` 에만 둔다. 이 저장소는 PUBLIC 이고,
> 폐쇄망 구성 자체가 정찰 정보다. `.gitignore` 도 CI 도 이건 못 막는다.

> **축소안 2번(마이그레이션 4→3단계)을 택하면** 아래 `v1.0/v1.2/v1.3` 을 `v1.0/v1.1/v1.2` 로,
> 스냅샷은 `v1.1-installed`/`v1.2-upgraded` 로 한 칸씩 내려 읽는다.

---

## 사람 개입 — 서로 합산하지 않는 칸들

| 지표 | 측정 방법 | 측정 시점 | 목표치 | 값 | 근거 |
| --- | --- | --- | --- | --- | --- |
| **패치 1건 개입 시간 (폐쇄망·업그레이드)** | 번들 생성 지시 → 적용 완료까지 사람이 손 댄 시간만 | **9번 v1.3 적용** | 10분 이내 | | |
| 패치 1건 개입 시간 (온라인) | 위와 동일 | VM-1 배포 시 | 승인 1회 | | |
| 최초 설치 개입 시간 (참고) | 신규 고객사 1곳 최초 구축 | 7번 ② | 고객사당 생애 1회 | | |
| **PoC 계측 오버헤드** | 스냅샷 촬영·복구, 시계 보정, **스냅샷 복구 후 재로드에 한한** 이미지 재로드, 양성/음성 대조, 덤프 대조, 증거 반출 | 발생할 때마다 누적 | 운영 비용 아님 — 별도 기록 | | |
| **운반 — PoC 실측 (사내 물리 복사)** | USB 꽂아 복사한 시간 | 7번 번들 이동 | 참고치 | | |
| **운반 — 고객사 반입 절차** | 반입 승인 · 보안 검사 · 담당자 일정 | **PoC 로 측정 불가** | `미측정 — 계약 전 고객사별 조사 필요` | 미측정 | 사내 랩에 반입 절차가 없다 |

> **릴리스마다 100곳에 반복되는 것은 업그레이드다.** 최초 설치는 고객사당 생애 1회이므로
> 그 값에 고객사 수 × 연 릴리스 횟수를 곱하면 계수가 처음부터 틀린다.
>
> **최초 `docker load` 는 운영 비용이다.** 오버헤드로 빼는 것은 *스냅샷 복구 때문에 다시 하는*
> 로드뿐이다. 수백 MB 로드가 3~5분이고 고객사 100곳 × 릴리스 1회면 300~500분이다.

## 번들·빌드

| 지표 | 측정 방법 | 측정 시점 | 목표치 | 값 | 근거 |
| --- | --- | --- | --- | --- | --- |
| 코어 이미지 크기 | `docker images` | #12 | chiseled base 184MB 가 하한 | | |
| **번들 크기 — 1개 버전** | 단일 버전 tar | #19 | 증분 배포 가능성의 근거 | | |
| **번들 크기 — 3개 묶음** | v1.0/v1.2/v1.3 묶음 tar | #19 | **실제 운반 비용.** 수백 MB 예상 | | |
| **로컬 이미지 점유량** | `docker load` 후 `du -sh /var/lib/docker` 증가분 | 7번 ② | tar 크기 ≠ 점유량. **롤백 상시 보관 요구량의 근거** | | |
| 코어 빌드 시간 | CI 파이프라인 소요 | #17 | 참고치 | | |
| 커스텀 빌드 시간 | base 재사용 시 증분 | #24 | 참고치 | | |
| 롤백 소요 시간 | 롤백 지시 → 정상 복귀 | 10번 | | | |

> 묶음이 1개 × 3 보다 많이 작지 않으면 레이어 공유가 안 되고 있다.

### 번들 적용 시간 — 적용 1회당 1행 (append)

| 일시 | 무엇 | 대상 버전 | 소요 (시작 → 헬스체크 통과) | 서비스 중단 여부 | 근거 |
| --- | --- | --- | --- | --- | --- |
| | 7번 ② 최초 설치 | v1.0 | | 중단할 서비스 없음 | |
| | 7번 ⑥ 멱등 2회차 | v1.0 | | | |
| | 9번 v1.2 정방향 | v1.2 | | | |
| | **9번 v1.3 업그레이드** | v1.3 | | **여기가 서비스 중단 시간** | |
| | 10번 롤백 후 기동 | v1.2 | | | |

## 계측 검증 — 통과해야 아래 H3 값이 의미를 갖는다

| 지표 | 측정 방법 | 측정 시점 | 목표치 | 값 | 근거 |
| --- | --- | --- | --- | --- | --- |
| OS 노이즈 baseline | 번들 미설치, 동일 시간 캡처 | 0단계 | 초과분 계산의 기준선 | | |

양성 대조·계측 생존은 **복구 직후마다** 수행하며 아래 복구 로그의 칼럼으로 기록한다.

## H3 — 폐쇄망

| 지표 | 측정 방법 | 측정 시점 | 목표치 | 값 | 근거 |
| --- | --- | --- | --- | --- | --- |
| **외부 통신 시도 (IPv4)** | baseline 초과분. `EGRESS-TRY4:` 유일 (DST, DPT) 조합 | **계수 창 A** | **0** | | |
| **외부 통신 시도 (IPv6)** | 같은 파이프, `EGRESS-TRY6:` | 계수 창 A | **0** | | |
| **DNS 질의 유일 이름** | 싱크홀 로그 | 계수 창 A | **0** | | |
| 재실행 안전성 (멱등) | **스냅샷 복구 없이** 연속 2회 적용 | 7번 ⑥ (계수 창 B) | 멱등 | | |
| **서명 검증 실패 시 중단 (a)** | 이미지 tar 페이로드 1바이트 변조 | 7번 ⑧ (계수 창 C) | exit≠0 + 서비스 미기동 + 로그에 `서명 불일치` | | |
| **서명 검증 실패 시 중단 (b)** | 다른 키쌍으로 전체 재서명 | 7번 ⑧ | 위와 동일 | | |
| 절차 우회 시 설치 성공 (c) | 검증 호출 제거 번들을 `verify-bundle.sh` 없이 실행 | 7번 ⑧ | **성공이 기대 결과** — 통제 수단이 절차임을 기록 | | |

### 계수 창 대장 — 창 1개당 1행

`plan.md` 7번 ① 이 "시작시각을 기록한다. **사후에 기억으로 채우면 안 된다**"고 못박은 값이 여기 들어간다.
아래 판정 명령의 `--since`/`--until` 이 이 표에서 나온다.

| 창 | 무엇을 재는가 | 시작시각 | 종료시각 | pcap 파일 | IPv4 계수 | IPv6 계수 | DNS 계수 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A | **H3 판정** (7번 ①~③) | | | `raw/` | | | |
| B | 멱등 2회차 (7번 ⑥) | | | `raw/` | 계수 대상 아님 | | |
| C | 음성 대조 3종 (7번 ⑧) | | | `raw/` | 계수 대상 아님 | | |

**창 A 만 H3 통신 계수의 대상이다.** B·C 의 egress 가 섞이면 H3 이 비-0 이 된다.

### 판정 명령

```sh
# IPv4 — 유일 목적지 (IP, 포트). 포트 없는 프로토콜은 PROTO 로 대체
journalctl _TRANSPORT=kernel --since "<창 시작>" --until "<창 종료>" \
  | grep 'EGRESS-TRY4:' \
  | awk '{d=p=r="";for(i=1;i<=NF;i++){if($i~/^DST=/)d=$i;else if($i~/^DPT=/)p=$i;else if($i~/^PROTO=/)r=$i}
          if(d!="")print d,(p!=""?p:r)}' \
  | sort -u | wc -l

# IPv6 — 같은 파이프, 접두어만 다르다
journalctl _TRANSPORT=kernel --since "<창 시작>" --until "<창 종료>" \
  | grep 'EGRESS-TRY6:' | awk '...위와 동일...' | sort -u | wc -l

# DNS 유일 질의 이름
awk '/query\[/{print $6}' /var/log/dnsmasq.log | sort -u | wc -l

# pcap 교차 확인 (IPv4 전용) — 필터 문자열 안에 백슬래시를 넣지 말 것
tcpdump -r raw/window-a.pcap 'tcp[tcpflags] & tcp-syn != 0 and not dst net 10.0.0.0/8 and not dst net 172.16.0.0/12 and not dst net 192.168.0.0/16 and not dst host 127.0.0.1'
```

**세 가지를 틀리기 쉽고, 셋 다 결과가 `0` 으로 보인다. 목표치도 `0` 이라 아무도 의심하지 않는다.**

- **`journalctl -k` 금지** — `--boot=0` 을 암시해서 `--since` 를 과거로 잡아도 현재 부팅분 밖은 안 나온다. `Storage=persistent` 로 살려둔 로그를 판정에서 스스로 버리게 된다
- **필터 문자열 안 백슬래시 금지** — 작은따옴표 안의 `\` 는 문자 그대로 libpcap 에 가서 `can't parse filter expression: syntax error`. `| wc -l` 로 감싸면 stderr 로 새고 `0` 이 찍힌다
- **`sed` 치환으로 뽑지 말 것** — `-n`/`p` 없이 쓰면 매칭 안 되는 줄(포트 없는 ICMP 등)이 타임스탬프째 통과해 계수가 부푼다. 위 `awk` 는 검증됨 (TCP 3 + ICMP 2 + UDP 1 패킷 → `3`)

**종료코드를 확인한다.** `wc -l` 은 앞단이 죽어도 `0` 을 찍는다. `set -o pipefail` 을 걸거나 단계를 따로 돌린다.

> 시도가 1건이라도 있으면 무엇인지 규명한다. NuGet, 이미지 pull, 라이선스 확인,
> 시간 동기화, **OCSP/CRL** 이 흔하다. 규명 결과를 적을 때 실주소를 쓰지 말고 자리표시자를 쓴다.

## H4 · H5 — 생애주기

| 지표 | 측정 방법 | 측정 시점 | 목표치 | 값 | 근거 |
| --- | --- | --- | --- | --- | --- |
| **덤프 정규화 자기 대조** | 동일 파이프로 `v1.2-installed` 를 2회 덤프 | 9번, H5 판정 **전** | **일치해야 H5 판정이 유효** | | |
| H4 데이터 유실 | 행 수 + 컬럼별 체크섬을 기대값 표와 대조 (전/후) | 9번 | 유실 없음 | | |
| H5 스키마 일치 | 정규화 후 덤프 vs `v1.2-installed` 참조본, `diff` 줄 수 | 10번 | 0줄 | | |
| H5 v1.3 전용 행 유실 | 9번에서 삽입한 N건의 잔존 | 10번 | 롤백 시 유실 후보는 사실상 이것뿐 | | |
| 마이그레이션 단계별 가역성 | 가역 / 손실 동반 가역 / 비가역 + 손실 항목 + **필요 DDL 권한** | 9번 | 표 자체가 산출물 | #25 산출물 | |

> 정규화 자기 대조 전에 H5 를 판정하면 도구 노이즈를 "역시 롤백 불가"로 읽는다.
> H5 는 실패 예상 가설이라 아무도 의심하지 않는다.

## H6 — 고객사 확장

| 지표 | 측정 방법 | 측정 시점 | 목표치 | 값 | 근거 |
| --- | --- | --- | --- | --- | --- |
| 보호 경로 수정 줄 수 | `git diff --stat -- <보호 경로>` | #18 | 0 | | |
| **신규 고객사 1곳 추가 사람 시간** | 매니페스트 작성 → 첫 파이프라인 성공 | #18 | H6 의 가치는 사실상 이 숫자 | | |
| **저장소 밖 수동 조작 건수** | Harbor 프로젝트·로봇 계정, CI 변수, replication 규칙 등 UI 클릭 | #18 | 저장소 0줄이어도 여기가 늘면 의미 없음 | | |

## 환경 구축 — 합산하지 않는 칸들

| 지표 | 측정 방법 | 측정 시점 | 목표치 | 값 | 근거 |
| --- | --- | --- | --- | --- | --- |
| **(a) 사내 1회성** | GitLab CE · Harbor A/B · VM-1 · 러너 | #4 | 고객사 수와 무관 | | |
| **(b) 고객사 1곳당 — VM 환경** | VM-2 상당 환경 구축 | #6 | | | |
| **(b) 중 앵커 주입분** | 공개키 + `verify-bundle.sh` 대역 외 주입만 | #6 | **키 회전 1회 = 고객사 수 × 이 값** | | |
| (b) 중 계측 설치분 | `DOCKER-USER` LOG · dnsmasq · tcpdump · journald | #6 | PoC 전용인지 상시 운영분인지 판단 근거 | | |

> **(b) 만 고객사 수에 곱한다.** (a) 를 합쳐 곱하면 100곳 추정에서 100배 과대계상된다.
> 앵커 주입분을 따로 뽑지 않으면 키 회전 비용이 약 20배 과대 추정된다 —
> `docs/findings.md` 첫 항목이 요구하는 계수가 이것이다.

## VM-2 스냅샷 — 촬영·복구 1회당 1행 (append)

`v1.0-installed` 복구만 최소 4회(7번 ⑦·⑨, 9번 v1.2 경로, 9번 v1.3 재복구)이고,
`v1.3-upgraded` 는 H5 재시도마다 더 늘어난다. 덮어쓰면 시계 오차 오인의 추적 근거가 사라진다.

| 일시 | 스냅샷 ID | 촬영/복구 | 시계 보정값 | 공개키 지문 | 양성 대조 IPv4 | 양성 대조 IPv6 | 계측 생존 | 이미지 재로드 소요 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| | `clean` | 촬영 | — | | | | | — |
| | | | | | | | | |

- **촬영/복구**: `촬영` 또는 `복구`
- **계측 생존**: `iptables -L DOCKER-USER -n -v` 규칙 잔존 + `systemctl is-active dnsmasq`
- 하이퍼바이저가 live snapshot(메모리 포함)인지 디스크 온리인지를 첫 행 근거에 적는다 — 복구 후 컨테이너 상태가 달라진다

> 복구는 시계도 되돌린다. NTP 가 막혀 있어 과거 시각이 유지되고, GPG·TLS 검증이
> 시계 오차로 실패하면 번들 문제로 오인하기 쉽다.
