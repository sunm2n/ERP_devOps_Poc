# ERP 배포 플랫폼 PoC 계획

작성일: 2026-08-21 전제 문서: [`erp-cicd-design-decisions.md`](http://erp-cicd-design-decisions.md) 상태: 초안

---

## 0. 이 PoC가 답하려는 질문

> **"지금 설계한 배포 구조가 실제로 동작하는가, 그리고 사람 손이 얼마나 필요한가."**

답하지 않는 질문은 아래와 같다. 보고서 서두에 명시할 것.

- 이 구조가 고객사 100곳에서 유지되는가 → PoC로 증명 불가
- 커스터마이징을 메타데이터로 얼마나 흡수할 수 있는가 → 코어 아키텍처 설계 영역
- Windows 고객사를 어떻게 처리할 것인가 → 사실 확인 및 계약 문제

**PoC 성공은 설계 검증이 아니라 "치명적 결함이 없음"의 확인이다.** 이 구분이 보고서에 없으면 미결 쟁점을 다시 꺼내기 어려워진다.

---

## 1. 검증 가설

각 가설은 참/거짓으로 판정 가능하도록 작성했다. 실패도 유효한 결과다.


| ID  | 가설                                             | 판정 기준                                 | 우선순위           |
| --- | ---------------------------------------------- | ------------------------------------- | -------------- |
| H1  | GitLab CI로 .NET 10 코어 이미지를 빌드해 Harbor에 올릴 수 있다 | 커밋 → 태그된 이미지가 Harbor에 존재              | 필수             |
| H2  | 커스텀 DLL을 코어 이미지 위에 레이어로 얹고 런타임에 로드할 수 있다       | 커스텀 로직이 실행되고, 코어 소스는 커스텀 저장소에 존재하지 않음 | 필수             |
| H3  | 오프라인 번들만으로 폐쇄망에서 설치·기동이 된다                     | 네트워크 차단 VM에서 외부 통신 0회로 서비스 정상 기동      | **최우선**        |
| H4  | 버전을 N개 건너뛴 환경에 누적 마이그레이션이 적용된다                 | v1.0 상태에 v1.3 번들 적용 성공, 데이터 유실 없음     | 필수             |
| H5  | 롤백이 가능하다                                       | 이미지 및 스키마를 직전 버전으로 복귀, 서비스 정상         | **필수 (실패 예상)** |
| H6  | 매니페스트 1개 수정만으로 신규 고객사 파이프라인이 생성된다              | 파이프라인 스크립트 수정 0줄                      | 필수             |
| H7  | Harbor replication으로 온라인 고객사에 이미지가 자동 전달된다     | Harbor A push → Harbor B 자동 반영        | 권장             |
| H8  | 폐쇄망에서 빌더 이미지로 `dotnet restore` 없이 빌드할 수 있다     | 네트워크 차단 상태에서 커스텀 빌드 성공                | 조건부            |


**H3을 최우선으로 두는 이유:** 완전 폐쇄망 고객을 받기로 결정한 이상, 오프라인 번들이 1급 산출물이라는 전제가 무너지면 나머지 설계가 전부 흔들린다. 가장 먼저, 가장 얕게라도 검증해야 한다.

**H5를 "실패 예상"으로 둔 이유:** 이미지 태그 되돌리기는 쉽지만 **역방향 DB 마이그레이션은 일반적으로 어렵다.** 여기서 "롤백 불가"라는 결론이 나오는 것 자체가 PoC의 가장 값어치 있는 산출물이 될 수 있다. 그 경우 대안(순방향 수정 전용 정책, 스냅샷 기반 복구)을 보고서에 제시한다.

**H8이 조건부인 이유:** 미결 M-3(커스텀 소스 보관 위치)과 M-4(고객사 개발자 PC 망 상태)에 의존한다. 커스텀 빌드를 우리 쪽에서 수행하는 것으로 확정되면 H8은 불필요하다. 확정 전이므로 보험 성격으로 포함한다.

---

## 2. 범위

### 포함

- 코어 애플리케이션: .NET 10 최소 웹 API (엔드포인트 2~3개, 헬스체크 포함)
- 확장 인터페이스 1개 (예: `IOrderValidator`) + 커스텀 구현체 1개
- DB 1종 + 마이그레이션 4단계 (v1.0 → v1.3)
- 고객사 2종 모의: 온라인형 1개, 완전 폐쇄망형 1개
- 커스텀 고객사 1곳

### 제외 (명시적으로 하지 않음)

- 실제 ERP 비즈니스 로직
- 메타데이터 엔진 / 표현식 평가기
- 쿠버네티스, ArgoCD, MSA 분리
- Windows 배포 경로
- Ansible 초기 설치 자동화 (수동 설치로 대체, 시간 측정만)
- 인증·권한, 모니터링, 로그 수집

---

## 3. 환경 구성


| 구성요소                     | 역할            | 비고                                         |
| ------------------------ | ------------- | ------------------------------------------ |
| GitLab CE (self-managed) | 코어 저장소 + CI   | Docker로 기동                                 |
| GitLab CE 2번째 그룹 또는 인스턴스 | 커스텀 저장소       | ADR-011 분리안 검증. 인스턴스 분리가 부담이면 그룹/권한 분리로 대체 |
| Harbor A                 | 사내 레지스트리      |                                            |
| Harbor B                 | 고객사 모의 레지스트리  | H7용                                        |
| VM-1                     | 온라인 고객사 모의    | 아웃바운드 허용                                   |
| VM-2                     | 완전 폐쇄망 고객사 모의 | **네트워크 완전 차단**                             |
| 배포 에이전트                  | 번들 수신·검증·적용   | PoC에서는 셸 스크립트로 충분                          |


**VM-2의 망 차단은 반드시 실제로 끊어야 한다.** 방화벽 규칙만 걸고 넘어가면 숨은 외부 의존성(NuGet, 이미지 pull, 라이선스 확인, 시간 동기화 등)을 못 잡는다. PoC에서 가장 흔한 실패 원인이 여기다.

---

## 4. 실행 단계

기간은 3주 기준. 단축 시 4단계를 먼저 버리고, 그다음 H7·H8을 버린다.

### 1단계 — 골격 (약 1주)

1. 샘플 코어 앱 작성. **12-factor 규칙을 처음부터 적용** (설정=환경변수, 로그=stdout, 로컬 디스크 저장 없음, 헬스체크, 그레이스풀 셧다운)
2. GitLab CI 파이프라인: 빌드 → 이미지 → Harbor push → **H1**
3. 매니페스트 파일 정의 + 공용 템플릿 `include` 구조 → **H6**
4. Harbor A/B replication 설정 → **H7**

### 2단계 — 폐쇄망 (약 1주)

5. 번들 생성 잡 추가: `docker save` + 설치 스크립트 + 체크섬 + 매니페스트를 tar로 패키징
6. VM-2로 번들 이동 (물리 복사), 설치 스크립트 실행 → **H3**
7. 커스텀 프로젝트 생성, `FROM erp-core:x.y.z` 레이어 빌드, 플러그인 로딩 확인 → **H2**

### 3단계 — 생애주기 (약 1주)

8. 마이그레이션 v1.0~v1.3 작성, VM-2를 v1.0에 고정 후 v1.3 번들 적용 → **H4**
9. v1.3 → v1.2 롤백 시도 (이미지 + 스키마) → **H5**
10. (조건부) 빌더 이미지로 VM-2 내부 빌드 → **H8**

### 4단계 — 측정·정리

11. 지표 수집, 실패 항목 원인 기록, 보고서 작성

---

## 5. 측정 지표

"됐다/안 됐다"만 적으면 보고서 가치가 낮다. 아래를 반드시 수치로 남긴다.


| 지표              | 측정 방법                             | 목표치                                 |
| --------------- | --------------------------------- | ----------------------------------- |
| 패치 1건의 사람 개입 시간 | 번들 생성 지시 → 적용 완료까지 사람이 손 댄 시간만 합산 | 온라인: 승인 1회 / 폐쇄망: 운반 제외 10분 이내      |
| **번들 크기**       | 생성된 tar 크기                        | **USB 운반과 고객사 전송 시간을 좌우. 수백 MB 예상** |
| 코어 빌드 시간        | CI 파이프라인 소요                       | 참고치                                 |
| 커스텀 빌드 시간       | base 재사용 시 증분                     | 참고치                                 |
| 번들 적용 시간        | 스크립트 실행 시작 → 헬스체크 통과              | 서비스 중단 시간과 직결                       |
| 롤백 소요 시간        | 롤백 지시 → 정상 복귀                     |                                     |
| 재실행 안전성         | 동일 번들 2회 적용                       | 멱등해야 함                              |
| 외부 통신 시도 횟수     | VM-2에서 네트워크 로그 확인                 | **0이어야 함**                          |


번들 크기가 예상보다 크면 대응책을 함께 검토한다. base 이미지 슬림화(chiseled/alpine 계열), 레이어 재사용을 통한 증분 번들, 코어 이미지와 커스텀 레이어의 분리 배포 등.

---

## 6. 산출물

1. 동작하는 GitLab CI 템플릿 1벌
2. 번들 생성 스크립트 + 설치 스크립트
3. 측정값 표 (5장 기준)
4. 실패 항목과 원인 목록
5. PoC 결과로 **확정된 ADR**과 **여전히 미결인 ADR** 구분표

5번이 핵심이다. PoC 이후 결정 문서가 어떻게 갱신되는지가 보고서의 결론이 되어야 한다.

---

## 7. 예상 리스크


| 리스크                   | 징후             | 대응                           |
| --------------------- | -------------- | ---------------------------- |
| 폐쇄망에서 숨은 외부 의존성 발견    | VM-2에서 기동 실패   | 실제 망 차단으로 조기 발견. 가장 흔한 실패 지점 |
| 번들 크기가 운반 불가 수준       | tar가 GB 단위     | 슬림 base, 증분 번들, 레이어 분리 검토    |
| 역방향 마이그레이션 불가         | H5 실패          | 순방향 전용 정책 + 스냅샷 복구로 정책 전환 검토 |
| 플러그인 로딩이 코어 수정을 요구    | H2에서 코어를 고치게 됨 | 확장 지점 설계가 PoC 이전에 필요하다는 신호   |
| PoC 성공이 설계 검증으로 과대 해석 | —              | 0장 한계 문구를 보고서 서두에 배치         |


---

## 8. 보고서 목차 (제안)

1. 배경과 PoC 목적
2. **PoC가 답하지 않는 것** (0장 인용)
3. 검증 가설과 결과 요약표
4. 측정값
5. 실패 항목과 원인
6. 설계 결정에 미치는 영향 (확정 / 미결 구분)
7. 다음 단계 권고

---

## 9. 미확정 항목

이 계획은 아래가 정해지면 조정이 필요하다.

- PoC 기간
- 모의 폐쇄망 VM 확보 가능 여부 (불가 시 H3·H8 검증 불가 → PoC 가치 대폭 하락)
- 보고서 대상 독자 (경영진이면 4·6장 중심, 기술팀이면 3·5장 중심)

