# ERP 배포 플랫폼 PoC 계획

작성일: 2026-08-21 전제 문서: `erp-cicd-design-decisions.md` (이 저장소에 없음 — 3주차 전까지 반입 필요, 9장 참조) 상태: 초안

---

## 0. 이 PoC가 답하려는 질문

> **"지금 설계한 배포 구조가 실제로 동작하는가, 그리고 사람 손이 얼마나 필요한가."**

답하지 않는 질문은 아래와 같다. 보고서 서두에 명시할 것.

- 이 구조가 고객사 100곳에서 유지되는가 → PoC로 증명 불가
- 커스터마이징을 메타데이터로 얼마나 흡수할 수 있는가 → 코어 아키텍처 설계 영역
- Windows 고객사를 어떻게 처리할 것인가 → 사실 확인 및 계약 문제

**PoC 성공은 설계 검증이 아니라 "치명적 결함이 없음"의 확인이다.** 이 구분이 보고서에 없으면 미결 쟁점을 다시 꺼내기 어려워진다.

---

## 1. 검증 가설

각 가설은 참/거짓으로 판정 가능하도록 작성했다. 실패도 유효한 결과다.


| ID  | 가설                                             | 판정 기준                                 | 우선순위           |
| --- | ---------------------------------------------- | ------------------------------------- | -------------- |
| H1  | GitLab CI로 .NET 10 코어 이미지를 빌드해 Harbor에 올릴 수 있다 | 커밋 → 태그된 이미지가 Harbor에 존재              | 필수             |
| H2  | 커스텀 DLL을 코어 이미지 위에 레이어로 얹고 런타임에 로드할 수 있다       | 커스텀 로직이 실행되고, 코어 소스는 커스텀 저장소에 존재하지 않음 | 필수             |
| H3  | 오프라인 번들만으로 폐쇄망에서 설치·기동이 된다                     | 계측 양성 대조 통과 후, baseline 초과 연결 시도 0건으로 서비스 정상 기동 + 변조 번들은 설치 중단 | **최우선**        |
| H4  | 버전을 N개 건너뛴 환경에 누적 마이그레이션이 적용된다                 | v1.0 상태에 v1.3 번들 적용 성공, 데이터 유실 없음     | 필수             |
| H5  | 롤백이 가능하다                                       | 마이그레이션 단계별 3분류(가역 / 손실 동반 가역 / 비가역) 표 완성 + 롤백 후 스키마 덤프 == 정방향 v1.2 덤프 | **필수 (실패 예상)** |
| H6  | 매니페스트 1개 수정만으로 신규 고객사 파이프라인이 생성된다              | 보호 경로에 대한 `git diff --stat` 이 비어 있음. 저장소 밖 수동 조작 건수와 소요 시간도 함께 기록 | 필수 |
| H7  | Harbor replication으로 온라인 고객사에 이미지가 자동 전달된다     | Harbor A push → Harbor B 자동 반영        | 권장             |
| H8  | 폐쇄망에서 빌더 이미지로 `dotnet restore` 없이 빌드할 수 있다     | 네트워크 차단 상태에서 커스텀 빌드 성공                | 조건부            |


**H3을 최우선으로 두는 이유:** 완전 폐쇄망 고객을 받기로 결정한 이상, 오프라인 번들이 1급 산출물이라는 전제가 무너지면 나머지 설계가 전부 흔들린다. 가장 먼저, 가장 얕게라도 검증해야 한다.

**H5를 "실패 예상"으로 둔 이유:** 이미지 태그 되돌리기는 쉽지만 **역방향 DB 마이그레이션은 일반적으로 어렵다.** 판정하려면 먼저 각 단계의 down 스크립트를 실제로 작성해 봐야 한다. 작성 과정에서 나오는 "이 단계는 되돌릴 수 없다"는 판단(컬럼 삭제, 타입 축소, 데이터 병합)이 이미 H5의 답 절반이다. 여기서 "롤백 불가"라는 결론이 나오는 것 자체가 PoC의 가장 값어치 있는 산출물이 될 수 있다. 그 경우 대안(순방향 수정 전용 정책, 스냅샷 기반 복구)을 보고서에 제시한다.

**H8이 조건부인 이유:** 미결 M-3(커스텀 소스 보관 위치)과 M-4(고객사 개발자 PC 망 상태)에 의존한다. 커스텀 빌드를 우리 쪽에서 수행하는 것으로 확정되면 H8은 불필요하다. 확정 전이므로 보험 성격으로 포함한다.

---

## 2. 범위

### 포함

- 코어 애플리케이션: .NET 10 최소 웹 API (엔드포인트 2~3개, 헬스체크 포함)
- 확장 인터페이스 1개 (예: `IOrderValidator`) + 커스텀 구현체 1개
- DB 1종 + 마이그레이션 4단계 (v1.0 → v1.3)
- 고객사 2종 모의: 온라인형 1개, 완전 폐쇄망형 1개
- 커스텀 고객사 1곳

### 제외 (명시적으로 하지 않음)

- 실제 ERP 비즈니스 로직
- 메타데이터 엔진 / 표현식 평가기
- 쿠버네티스, ArgoCD, MSA 분리
- Windows 배포 경로
- Ansible 초기 설치 자동화 (수동 설치로 대체, 시간 측정만)
- 인증·권한, 모니터링, 로그 수집

---

## 3. 환경 구성


| 구성요소                     | 역할            | 비고                                         |
| ------------------------ | ------------- | ------------------------------------------ |
| GitLab CE (self-managed) | 코어 저장소 + CI   | Docker로 기동                                 |
| GitLab CE 2번째 그룹 또는 인스턴스 | 커스텀 저장소       | ADR-011 분리안 검증. 인스턴스 분리가 부담이면 그룹/권한 분리로 대체 |
| Harbor A                 | 사내 레지스트리      |                                            |
| Harbor B                 | 고객사 모의 레지스트리  | H7용                                        |
| VM-1                     | 온라인 고객사 모의    | 아웃바운드 허용                                   |
| VM-2                     | 완전 폐쇄망 고객사 모의 | **네트워크 완전 차단**                             |
| 배포 에이전트                  | 번들 수신·검증·적용   | PoC에서는 셸 스크립트로 충분                          |


**VM-2의 망 차단은 반드시 실제로 끊어야 한다.** 방화벽 규칙만 걸고 넘어가면 숨은 외부 의존성(NuGet, 이미지 pull, 라이선스 확인, 시간 동기화, **인증서 폐기 확인(OCSP/CRL)** 등)을 못 잡는다. PoC에서 가장 흔한 실패 원인이 여기다.

**다만 "끊는 것"과 "시도를 관측하는 것"은 별개다.** 완전히 끊어버리면 시도 자체를 볼 수 없어, 앱이 외부에 붙으려다 타임아웃 후 기동한 경우까지 "통신 0회 성공"으로 잘못 판정한다. 정확히 PoC가 잡아야 할 케이스가 그것이다. **차단 계층과 관측 계층을 분리한다.**

**차단은 VM 바깥에서, 격리 네트워크 + 블랙홀 게이트웨이로 한다.** VM 안의 `iptables` 는 차단 수단이 아니라 관측 보조다 — 방화벽 규칙만 걸고 넘어가는 것이 81행이 경고한 그 실패이고, 규칙은 스냅샷 복구에 살아남지 않아 조용히 망이 열린 채로 재측정될 수 있다.

**가상 NIC 를 떼는 방식은 쓰지 않는다.** NIC 가 없으면 VM-2 에 목적지 경로가 없어, 컨테이너가 외부로 SYN 을 쏘면 라우팅 결정 단계에서 `EHOSTUNREACH` 로 폐기된다. netfilter forward 훅은 **경로 조회 성공 이후에만** 실행되므로 `FORWARD`/`DOCKER-USER` 가 순회조차 되지 않는다. pcap 에는 veth 에서 잡히는데 iptables 로그만 조용히 0 이 되는 **반쪽 계측**이 된다. VM-2 는 default route 와 ARP 응답하는 게이트웨이 IP 를 **계속 가지되**, 그 게이트웨이가 외부로 포워딩하지 않는 구성이어야 한다.

**관측은 VM-2 내부에서, 아래를 전부 갖춘다.**

- **체인 위치.** 앱은 컨테이너로 뜨므로 egress 는 호스트 filter `OUTPUT` 을 **지나지 않는다.** `FORWARD` (Docker 가 선점하므로 사용자 훅인 `DOCKER-USER`)를 지나 `nat POSTROUTING` 에서 MASQUERADE 된다. `OUTPUT` 에만 걸면 한 줄도 안 남는다.
- **목적지 범위 제한.** `DOCKER-USER` 는 컨테이너↔컨테이너 트래픽도 순회한다(docker 가 `bridge-nf-call-iptables=1` 을 켠다). 범위를 안 걸면 **앱→DB SYN 이 전부 시도로 계수되어 H3 이 구조적으로 0 이 될 수 없다.** 사설 대역을 제외한다:
  `iptables -I DOCKER-USER ! -d 10.0.0.0/8 ! -d 172.16.0.0/12 ! -d 192.168.0.0/16 -j LOG --log-prefix "EGRESS-TRY4: "`
- **규칙 순서.** LOG 는 DROP **앞**에 둔다(`-I`). DROP 은 순회를 종료시키므로 뒤에 append 한 LOG 에는 도달하지 않는다. `OUTPUT` 에 정책을 걸 때는 `lo` 를 예외로 둔다 — 루프백까지 막으면 헬스체크 `curl localhost` 가 죽고 실행자는 앱 문제로 오인한다.
- **IPv6.** `ip6tables` 에 동일하게 적용하되 **접두어는 `EGRESS-TRY6: ` 로 고정한다**(제외 대역은 `fc00::/7`, `fe80::/10`). 접두어를 고정하지 않으면 판정 grep 이 한 줄도 못 잡고 IPv6 행이 조용히 `0` 으로 찬다 — 하필 진짜 시도가 가장 많이 날 경로다. .NET `HttpClient` 는 듀얼스택에서 IPv6 를 먼저 시도하므로, 빠뜨리면 AAAA 로 해석된 목적지는 차단도 로깅도 안 된다.
- **DNS.** 질의 전량을 기록하는 싱크홀 리졸버(dnsmasq `log-queries`)를 VM-2 안에 둔다. `resolv.conf` 가 비었거나 `systemd-resolved` 를 가리키면 질의가 물리 인터페이스에 나가지 않고 즉시 `EAI_NONAME` 으로 끝나 **와이어에 아무것도 안 남는다.**
- **패킷 캡처.** `tcpdump -i any -C 100 -W 10 -w` 로 상한과 회전을 건다(최대 약 1GB — `clean` 이 이만큼의 여유 디스크를 갖도록 0단계에서 확인). 상한 없는 캡처가 VM-2 디스크를 채우면 측정 대상 서비스 자체가 죽는다.
- **로그 영속화.** journald 를 `Storage=persistent` 로. volatile 이면 설치·업그레이드 중 재부팅 한 번에 증거가 사라지고, **결과는 또 "0건"으로 보인다.**

**계측을 먼저 검증한다 — 양성 대조.** 위 장치 중 하나라도 잘못 걸리면 결과는 똑같이 "0건"이고, 그 0을 H3 의 근거로 쓰게 된다. **본 측정 전에** 컨테이너 안에서 외부 IP 로 의도적 연결을 1회 시도하고, 그것이 iptables 로그와 pcap **양쪽에** 나타나는 것을 확인한다. 이 절차가 없으면 "0건"은 증거가 아니라 정황이다.

- 대조에 쓸 최소 이미지(busybox 등)를 **망 차단 전에** 확보해 `clean` 에 포함시킨다. 앱 이미지는 1단계에 나오고 번들은 2단계에 들어오므로, 0단계 시점에는 대조할 컨테이너가 없다.
- **`clean` 복구 후에는 매번 재수행한다.** 1회 확인으로는 재측정된 "0건"의 근거가 첫날뿐이다. 복구 직후 `iptables -L DOCKER-USER -n -v` 와 `systemctl is-active dnsmasq` 로 계측 생존도 함께 기록한다.
- **제외 대역 자체도 대조 대상이다.** 너무 넓게 잡으면 진짜 시도까지 지워지고 결과는 다시 "0건"으로 보인다. 대조용 연결은 제외 대역 **밖**의 공인 IP 로 쏜다.

**OS 노이즈 baseline.** NTP·패키지 데몬 등 상시 트래픽을 세면 절대 0 이 안 되고, 필터로 지우면 진짜 시도까지 지운다. `clean` 상태에서 번들 미설치로 동일 시간 캡처를 뜨고, H3 지표는 **baseline 초과분**으로 정의한다.

**계수 단위.** TCP 연결 1회는 SYN 재전송으로 5~6패킷이 된다. **"목적지 (IP, 포트) 유일 조합 + DNS 질의 유일 이름"** 으로 센다. 판정 명령을 계획에 박는다 — pcap 을 눈으로 보는 건 절차가 아니다. 사람마다 다른 답이 나온다.

**계수 창의 시작·종료 시각을 먼저 기록한다**(7번 ①). 아래 `--since`/`--until` 이 그 값이다.

```sh
# IPv4 — 유일 목적지 (IP, 포트) 조합. 포트 없는 프로토콜은 PROTO 로 대체한다
journalctl _TRANSPORT=kernel --since "<창 시작>" --until "<창 종료>" \
  | grep 'EGRESS-TRY4:' \
  | awk '{d=p=r="";for(i=1;i<=NF;i++){if($i~/^DST=/)d=$i;else if($i~/^DPT=/)p=$i;else if($i~/^PROTO=/)r=$i}
          if(d!="")print d,(p!=""?p:r)}' \
  | sort -u | wc -l

# IPv6 — 같은 파이프, 접두어만 다르다
journalctl _TRANSPORT=kernel --since "<창 시작>" --until "<창 종료>" \
  | grep 'EGRESS-TRY6:' | awk '...위와 동일...' | sort -u | wc -l

# pcap 교차 확인 (IPv4 전용). 필터 문자열 안에 줄바꿈용 백슬래시를 넣지 말 것
tcpdump -r raw/<창>.pcap 'tcp[tcpflags] & tcp-syn != 0 and not dst net 10.0.0.0/8 and not dst net 172.16.0.0/12 and not dst net 192.168.0.0/16 and not dst host 127.0.0.1'

# DNS — 싱크홀 로그의 유일 질의 이름
awk '/query\[/{print $6}' /var/log/dnsmasq.log | sort -u | wc -l
```

**세 가지를 틀리기 쉽다. 셋 다 결과가 `0` 으로 보이고, 목표치도 `0` 이라 아무도 의심하지 않는다.**

- **`journalctl -k` 를 쓰지 않는다.** `-k` 는 `--boot=0` 을 암시해서 `--since` 를 아무리 과거로 잡아도 현재 부팅분 밖은 나오지 않는다. 재부팅 대비로 `Storage=persistent` 를 걸어놓고 판정 단계에서 그 로그를 스스로 버리게 된다. `_TRANSPORT=kernel` 을 쓴다.
- **필터 문자열 안에 백슬래시를 넣지 않는다.** 작은따옴표 안의 `\` 는 셸의 줄 연결이 아니라 문자 그대로 libpcap 에 전달되어 `can't parse filter expression: syntax error` 가 난다. `| wc -l` 로 감싸면 **에러는 stderr 로 새고 stdout 은 비어 `0` 이 찍힌다.**
- **`sed` 로 뽑지 않는다.** `-n`/`p` 없이 치환하면 매칭 안 되는 줄(포트 없는 ICMP 등)이 원문 그대로 통과해 타임스탬프째 `sort -u` 를 빠져나간다. 패킷 수만큼 계수가 부푼다. 위 `awk` 는 유일 목적지를 정확히 센다(검증: TCP 3패킷 + ICMP 2패킷 + UDP 1패킷 → `3`).

**명령의 종료코드를 확인한다.** `wc -l` 은 앞단이 죽어도 `0` 을 찍는다. `set -o pipefail` 을 걸거나 각 단계를 따로 돌려 exit 를 본다.

**pcap 원본은 저장소에 올리지 않는다.** DB 접속 시도, 호스트명, 내부 IP 가 들어간다. 요약 수치만 기록한다.

**VM-2 는 스냅샷으로 상태를 관리한다.** H3(설치) → H4(업그레이드) → H5(롤백)가 같은 VM 을 순차로 오염시키므로, 각 검증 전 baseline 으로 복귀할 수 없으면 결과를 신뢰할 수 없다. 스냅샷 4개를 유지한다.

| 스냅샷 | 상태 | 찍는 시점 |
| --- | --- | --- |
| `clean` | OS + 컨테이너 런타임 + 망차단·로깅 규칙 **영구화** + 대조용 최소 이미지 + **GPG 공개키와 검증 스크립트 사전 배치** | 0단계 종료 |
| `v1.0-installed` | v1.0 설치 1회 + **시드 데이터 적재** 완료 | 7번 ⑤ |
| `v1.2-installed` | 정방향으로 설치한 v1.2 (H5 의 스키마 참조본) | 9번, v1.2 적용 직후 · `v1.0-installed` 재복귀 전 |
| `v1.3-upgraded` | v1.3 업그레이드 + **v1.3 전용 행 삽입** 완료 | 9번 종료, 행 삽입 **후** |

`clean` 을 "OS 만"으로 정의하면 안 된다. 망이 끊긴 VM-2 에서는 컨테이너 런타임을 다시 설치할 방법이 없고(`apt install` 이 안 된다), iptables 규칙이 복구에 살아남지 않으면 망이 열린 상태로 H3 을 재측정하게 된다.

**스냅샷 복구는 디스크 전체를 되돌린다 — DB 만이 아니다.** 번들 tar, `docker load` 한 이미지, 스키마 덤프, pcap, journald 로그가 함께 사라진다. 9번은 `v1.0-installed` 로 두 번 복귀하므로, 대책이 없으면 v1.2 이미지가 소실되어 **7장의 "v1.2 이미지 부재" 리스크가 운반이 아니라 스냅샷 경로로 되살아난다.**

- **번들 tar 는 스냅샷 범위 밖의 별도 볼륨에 둔다.** 시스템 디스크만 스냅샷 대상으로 한다. 그러지 않으면 복구할 때마다 USB 재운반이다.
- **이미지는 볼륨에 옮길 수 없다.** `docker load` 한 이미지는 데몬 storage root(`/var/lib/docker`) 안에 있다. data-root 를 통째로 별도 볼륨으로 옮기면 **DB 컨테이너 볼륨까지 스냅샷 밖으로 나가 `v1.0-installed`·`v1.2-installed` 가 스키마 상태를 담지 못하고** 3장 상태관리 전체가 무너진다. 따라서 **별도 볼륨에는 이미지 tar 만 두고, 복구할 때마다 `docker load` 를 다시 한다.** 복구 1회당 로드 시간이 붙으므로 PoC 계측 오버헤드에 계상한다.
- **덤프·pcap·로그는 복구 전에 VM-2 밖으로 반출한다.** 폐쇄망이라 반출도 물리 복사이고 시간이 든다. 반출하지 않으면 `Storage=persistent` 를 걸어놓고 스냅샷 롤백으로 지우는 셈이 된다.

**스냅샷 복구는 시계도 되돌린다.** H3 때문에 NTP 가 막혀 있으므로 과거 시각이 그대로 유지된다. GPG 서명이나 TLS 검증이 시계 오차로 실패하면 번들 문제로 오인하기 쉽다. 복구 후 시계를 수동 보정하고 기록한다. 하이퍼바이저가 live snapshot(메모리 포함)인지 디스크 온리인지도 기록한다 — 복구 후 컨테이너 상태가 달라진다.


## 4. 실행 단계

**배분 합계는 18~21 영업일로, 3주(15 영업일)를 3~6일 초과한다.** 이건 계산 착오가 아니라 이 계획의 결론이다. 9장에서 아래 중 하나를 확정해야 한다.

- **4주로 늘린다** — 다만 4주(20영업일)로도 상한 21일에는 최악 1일 모자란다. 여유 있는 선택이 아니다, 또는
- **축소 1·2·3번을 선반영한 3주** — 그래도 약 15.5일이라 여유가 없다

2인을 넣어도 크게 줄지 않는다. 병렬이 먹히는 건 0단계(인프라 vs VM-2 관측)와 1단계(앱·시드 vs CI·매니페스트)뿐이고, **2·3단계는 VM-2 1대를 직렬로 점유한다** — 스냅샷 복구 중에 다른 사람이 같은 VM 에서 작업할 수 없다. 2인 효과는 최대 2~3일이고, H5 는 실패 예상이라 재시도 여유가 필요한데 그 여유가 0 이다.

| 단계 | 배분 | 내용 |
| --- | --- | --- |
| 0단계 | 2~4일 | 환경 구축 |
| 1단계 | 5일 | 골격 + 시드·기대값표 + 마이그레이션 up 4종 |
|  |  | *up 이 3단계에서 넘어왔다. 5일 안에 들어간다는 근거가 없으면 1단계 6일 / 3단계 3일로 재배분할 것* |
| 2단계 | 5일 | 폐쇄망 |
| 3단계 | 4일 | 생애주기 |
| 4단계 | **2~3일** | 측정·정리 |
| **합계** | **18~21일** | **3주(15영업일) 를 3~6일 초과 — 9장에서 확정** |

**축소가 필요하면 아래 순서로 버린다. 4단계는 버리지 않는다.**

1. **VM-1 + H7 을 함께** — VM-1 은 온라인 경로/H7 전용이다. 묶어서 버리면 검증 하나가 아니라 환경 구축 1~1.5일이 통째로 빠진다
2. **마이그레이션 4단계 → 3단계** — H4 의 "N개 건너뛴"은 v1.0→v1.2(2홉)로도 성립한다. up/down 8종이 6종이 된다. 가설을 버리는 게 아니라 **표본을 줄이는 것**이라 H8·H2 보다 먼저 온다
   *(이 축소를 택하면 최고 버전이 v1.2 이므로 10번은 **v1.2→v1.1 롤백**이 되고, 스냅샷은 `v1.1-installed` / `v1.2-upgraded` 로 읽는다. 6번의 tar 3종 버전과 9번의 "v1.3 전용 행", 5·7장의 버전 표기도 한 칸씩 내려 읽는다. 3단계 절차를 다시 쓰지 않아도 되도록 여기 미리 적어 둔다 — 축소는 시간이 없을 때 내리는 결정이고, 바로 그때 절차를 재작성할 시간이 없다.)*
3. **H8** (조건부)
4. **H2 커스텀 범위 축소** — 마지막이다. 인터페이스 정의를 1단계로 당겨놨으므로(1번) H2 를 버려도 "플러그인 로딩이 코어 수정을 요구" 리스크는 절반 검증된 채 남는다

4단계는 측정·원인기록·보고서이고 6장 산출물 3·4·5번이 전부 거기서 나온다. **축소 순서에서 지켜도 달력에서 지워지면 결과는 같다** — 3단계가 밀리면 4단계는 축소 결정 없이 조용히 압축되고, 마지막 날 밤에 측정값을 기억으로 채우게 된다.

### 0단계 — 환경 구축 (2~4일)

3장의 환경은 저절로 생기지 않는다. 1단계 5일은 앱·시드·up 4종·파이프라인·매니페스트만으로 이미 꽉 찬다.

0. GitLab CE + 커스텀 저장소(그룹/인스턴스 분리) + Harbor A/B + VM-1 + VM-2 구축
   - VM-2 는 **격리 네트워크 + 블랙홀 게이트웨이**로 망을 끊고(가상 NIC 제거는 쓰지 않는다 — 3장 참조), 안에는 관측 장치(`DOCKER-USER` LOG + 사설대역 제외, `ip6tables`, DNS 싱크홀, tcpdump 회전, journald 영속화)를 건다
   - **망 차단 전에** 대조용 최소 이미지(busybox 등)를 확보한다. 차단 후에는 받을 방법이 없다
   - **양성 대조 1회 수행** — 제외 대역 밖 공인 IP 로의 의도적 연결이 iptables 로그와 pcap 양쪽에 잡히는 것을 확인. 이게 통과해야 H3 측정이 의미를 갖는다
   - OS 노이즈 baseline 캡처
   - GPG 키쌍 생성. **공개키와 검증 스크립트(`verify-bundle.sh`)를 VM-2 에 대역 외로 주입**한다. 공개키만 밖으로 빼는 것은 앵커의 절반이다 — 앵커를 *사용하는 코드*가 번들 안에서 오면 변조자가 `gpg --verify` 호출을 삭제하면 그만이고, 사전 배치한 키는 아무도 읽지 않는다
   - 번들·이미지를 둘 **별도 볼륨**을 스냅샷 범위 밖에 마련한다
   - 규칙·설정 영구화 후 `clean` 스냅샷 촬영
   - 값은 `measurements/metrics.md` 에 적는다. **소요를 (a) 사내 인프라 1회성 / (b) 고객사 측 환경 1곳당 으로 나눠 기록하고, (b) 안에서 앵커 주입분(공개키 + `verify-bundle.sh`)을 또 쪼갠다** — 키 회전 1회 비용이 `고객사 수 × 앵커 주입` 이라 그 계수만 따로 필요하다. 합산 숫자를 "고객사 1곳당 비용"으로 쓰면 100곳 추정에서 100배 과대계상된다. 고객사 1곳당 실제로 필요한 건 VM-2 상당 환경 + 공개키·검증 스크립트 주입뿐이다

### 1단계 — 골격 (5일)

1. 샘플 코어 앱 작성. **12-factor 규칙을 처음부터 적용** (설정=환경변수, 로그=stdout, 로컬 디스크 저장 없음, 헬스체크, 그레이스풀 셧다운)
   - 확장 인터페이스 `IOrderValidator` 와 플러그인 로딩 지점을 **여기서** 정의한다. 2단계 8번(H2)의 선행이다.
   - 코어를 고치지 않고 확장이 가능한지가 곧 7장의 "플러그인 로딩이 코어 수정을 요구" 리스크 검증이다.
   - **시드 데이터셋을 여기서 만든다.** 주문·거래처 각 수백 건, **타입 축소 대상 컬럼에 경계값 포함**. H4·H5 의 "데이터 유실 없음"은 비교 대상이 있어야만 판정된다 — 빈 DB 면 잃을 게 없어 두 가설이 자동으로 참이 되고, down 스크립트 작성이 전부 헛돈다.
   - **컬럼별 기대값 표를 시드와 함께 만든다.** 타입 축소가 일어나면 체크섬은 당연히 바뀐다. 어떤 변화가 "정상 변환"이고 어떤 것이 "유실"인지 미리 정의하지 않으면 9·10번의 덤프 대조를 판정할 수 없다.
2. **마이그레이션 v1.0~v1.3 정방향(up) 작성.** VM-2 없이 개발 PC 에서 되는 작업이고, **2단계 6번이 v1.2·v1.3 번들을 포장하려면 그 내용물이 여기서 완성돼 있어야 한다.** down 작성과 실행·판정은 3단계 9번이다.
3. GitLab CI 파이프라인: 빌드 → 이미지 → Harbor push → **H1**
   - 이미지 **다이제스트**를 기록한다. 태그 기반 롤백은 Harbor 에서 태그 덮어쓰기가 허용되면 다른 이미지를 가리킬 수 있고, 재현성(같은 커밋 → 같은 이미지)의 근거도 다이제스트다.
4. 매니페스트 파일 정의 + 공용 템플릿 `include` 구조 → **H6**
   - **보호 경로 집합을 여기서 확정한다** (`ci/templates/**`, `.gitlab-ci.yml`, `scripts/**` 등). H6 판정은 고객사 2호 추가 커밋에 대해 `git diff --stat -- <보호 경로>` 가 비어 있는지로 한다. 사람 눈으로 "이건 수정인가"를 다투지 않는다.
   - 고객사 등록이 **파일 추가만으로 되는지, 목록 갱신이 필요한지**를 명시한다. `include: local: 'manifests/*.yml'` 같은 와일드카드면 진짜 0줄이고, 목록 파일에 한 줄 추가하는 방식이면 H6 는 자동으로 거짓이다. 즉 H6 의 결과는 측정이 아니라 **여기서의 설계 선택**으로 결정된다.
   - 매니페스트에는 **가명(`customer-a`)과 더미 주소만** 쓴다. 이 저장소는 public 이다.
5. Harbor A/B replication 설정 → **H7**

### 2단계 — 폐쇄망 (5일)

6. 번들 생성 잡 추가: `docker save` + 설치 스크립트 + 체크섬 + **서명** + 매니페스트를 tar 로 패키징
   - 체크섬은 전송 오류를 잡고 **서명은 변조를 잡는다.** USB·파일전송을 거치는 순간 신뢰 경계를 넘으므로 ERP 배포에서는 후자가 필요하다. GPG detached signature 로 충분하다.
   - **신뢰 앵커는 번들 밖에 있어야 한다.** 공개키가 번들 안에 들어가면 변조자가 이미지에 백도어를 넣고 자기 키로 재서명한 뒤 tar 안의 공개키를 함께 바꿔치기하면 그만이다. **검증 코드도 마찬가지다** — 번들 안 `install.sh` 의 `gpg --verify` 호출을 삭제하면 사전 배치한 공개키는 아무도 읽지 않는다. 어느 쪽이든 **검출력은 체크섬만 있을 때와 같아진다.**
   - 따라서 운영자는 **VM-2 에 사전 배치된 `verify-bundle.sh` 를 먼저 실행**하고, 그것이 통과한 뒤에만 번들 안의 설치 스크립트를 실행한다. **번들 안 설치 스크립트도 서명 대상에 포함된다.**
   - 검증 실패 시 **실제로 중단**되어야 한다. 중단 시 (a) 실패 원인 구분(체크섬 불일치 / 서명 불일치 / 공개키 없음 / 키 만료), (b) 기대 키 ID 와 실제 서명자, (c) 로그 파일 절대 경로를 화면과 파일 양쪽에 남긴다. 폐쇄망 고객사 담당자는 "설치가 안 돼요"라고만 말할 수 있다.
   - 번들은 **자기 신원**을 갖는다: 버전, 빌드 커밋 SHA, 이미지 다이제스트, 생성 시각, 서명자. 설치 스크립트는 적용 기록을 VM 에 append 한다. 감사 추적 이전에 **멱등·H4 판정 수단**이다.
   - **v1.0 / v1.2 / v1.3 을 단일 tar 로 묶어 `docker save` 한다.** 셋은 같은 base 를 공유하므로 따로 저장하면 base 크기 × 3 이 되어 5장 핵심 지표(번들 크기)가 측정 방식 때문에 부풀려진다. **1개 번들 크기와 3개 묶음 크기를 따로 기록한다** — 후자가 실제 운반 비용이고 전자가 증분 배포 가능성의 근거다.
   - **운반·서명·설치의 단위를 확정한다: 묶음 tar 1개가 곧 번들 1개다.** 그 안에 v1.0·v1.2·v1.3 이미지와 각 버전의 설치 스크립트·매니페스트가 들어가고, **서명은 묶음 tar 전체에 1회** 건다. "번들 3종"은 버전 3개를 담았다는 뜻이지 파일 3개가 아니다 — 파일을 나누면 base 가 3번 들어가 크기 지표가 부풀고, 음성 대조 (a)가 뒤집을 "안의 이미지 tar"가 어느 파일에 있는지도 불명확해진다.
   - 폐쇄망에는 `docker pull` 이 없다. 롤백은 직전 이미지가 로컬에 있을 때만 성립하므로 v1.2 를 빠뜨리면 10번이 실행 불가다.
7. **번들 3종을 물리 복사로 VM-2 의 별도 볼륨(스냅샷 범위 밖)에 옮기고**, 아래 순서로 진행한다 → **H3**
   1. **계수 창 A 시작** — 시작시각을 기록하고 tcpdump 를 띄운다. 판정 명령의 `--since` 는 여기서 나온다. 사후에 기억으로 채우면 안 된다
   2. v1.0 을 `verify-bundle.sh` 로 검증 후 정상 설치 → **헬스체크 통과 확인**
   3. **여기서 H3 을 판정한다.** 계수 창 A 를 닫고 판정 명령(3장)을 돌려 baseline 초과분을 센다. IPv4 는 pcap + `journalctl -k`, IPv6 는 `ip6tables` 로그. **이 창이 H3 통신 계수의 유일한 대상이다** — 아래 ⑥·⑧ 의 egress 는 여기 섞이면 안 된다
   4. 시드 데이터 적재
   5. **`v1.0-installed` 스냅샷 촬영** (이 상태가 9번의 출발점이다 — 적용 1회 + 시드)
   6. **멱등성 확인**: 복구 없이 v1.0 을 **1회 더** 적용(누적 2회). 스냅샷 복구 후 재적용은 아무것도 검증하지 않는다. **별도 계수 창**
   7. `v1.0-installed` 복구 → 시계 보정 → 이미지 재로드 → **양성 대조 재수행(IPv4·IPv6 각 1회)** → `iptables -L DOCKER-USER -n -v` / `systemctl is-active dnsmasq` 로 계측 생존 확인 → 기록
   8. **음성 대조 3종. 별도 계수 창이며 통신 계수 대상이 아니다** — 이건 서명 검증 판정이다
      - **(a) 페이로드 1바이트 변조** — tar 구조는 유효하게 둔 채 **안의 이미지 tar 페이로드** 1바이트를 뒤집는다. 아무 데나 뒤집으면 `tar xf` 가 먼저 깨져 exit≠0 이 나오고, `gpg --verify || true` 가 들어 있어도 대조가 통과한다. **판정: exit≠0 + 서비스 미기동 + 로그에 `서명 불일치`**
      - **(b) 다른 키쌍으로 전체 재서명** (번들 안 공개키까지 교체). (a)만으로는 대역 외 앵커가 실제로 쓰이는지 알 수 없다 — 변조하면 어느 키로 검증하든 서명이 깨지므로 번들 안 공개키를 쓰는 스크립트도 (a)를 통과시킨다. **판정: 위와 동일**
      - **(c) 절차 우회 관측** — `install.sh` 의 검증 호출을 제거한 번들을 `verify-bundle.sh` **없이** 실행한다. **여기서는 설치가 그대로 성공하는 것이 기대 결과다.** (a)·(b)는 앵커 *키* 를 검증할 뿐 앵커 *코드* 를 검증하지 않는다 — "검증 코드가 번들 안에서 온다"는 위협의 실제 통제 수단은 대조가 아니라 **`verify-bundle.sh` 선행 실행이라는 절차 그 자체**다. (c)는 그 사실을 기록으로 남긴다. 절차를 모르는 고객사 담당자가 `install.sh` 만 돌리면 검증 없이 설치된다는 것이 측정된 사실이 되어야, 7장 리스크의 대응이 "절차 고정 + 교육"임을 보고서에서 주장할 수 있다
   9. `v1.0-installed` 복구. **덤프·pcap·journald 로그는 복구 전에 반출** — 복구는 증거도 지운다
   - **각 단계의 값을 그 자리에서 `measurements/metrics.md` 에 적는다.** 계수 창 시작·종료 시각, 스냅샷 촬영·복구 1회당 1행, 측정값과 근거. 나중에 몰아서 하려면 v1.0 설치부터 다시 해야 한다
8. 커스텀 프로젝트 생성, `FROM erp-core:x.y.z` 레이어 빌드, 플러그인 로딩 확인 → **H2**

### 3단계 — 생애주기 (4일)

9. 마이그레이션 **역방향(down) 작성**(up 은 1단계 2번에서 완료). 단계별 표를 만든다 — **되돌릴 수 있는가(가역 / 손실 동반 가역 / 비가역)**, 손실 항목, **필요 DDL 권한**. 권한 열이 없으면 "롤백 가능"이라는 결론에 딸려 오는 대가(고객사 DB 에 파괴적 DDL 권한 계정이 상시 존재)를 6장 구분표에 올릴 근거가 없다.
   - **덤프 정규화 파이프를 먼저 고정한다.** `pg_dump --schema-only --no-owner --no-privileges --no-comments` 등으로 도구 노이즈(나열 순서, `SET` 헤더, 소유자·권한 구문, 생성 시각 주석, 시퀀스 `last_value`)를 제거하고 정렬한다. 판정은 바이트 일치가 아니라 **정규화 후 `diff` 줄 수**다
   - **정규화 파이프의 양성 대조**: `v1.2-installed` 를 찍을 때 동일 파이프로 **두 번 떠서 자기 자신과 일치**하는지 먼저 확인한다. 이게 없으면 H5 는 도구 노이즈로 불일치해도 **"역시 롤백 불가"로 조용히 통과한다** — 실패 예상 가설이라 아무도 의심하지 않기 때문이다
   - `v1.0-installed` 복구 → v1.2 정방향 적용 → **`v1.2-installed` 스냅샷 + 정규화 덤프 확보** (10번의 참조본. 덤프는 복구 전에 반출)
   - `v1.0-installed` 재복구 → v1.3 번들 적용 → **H4**. 적용 전/후 행 수와 컬럼별 체크섬을 1단계의 기대값 표와 대조한다
   - **v1.3 스키마에서만 유효한 행을 N건 삽입한다.** 9번이 v1.0 에서 v1.3 으로 직행하므로, 이걸 안 하면 10번이 검사할 "v1.2 이후에 쓰인 행"이 **공집합**이고 유실 검사가 자동 통과한다
   - 행 삽입 **후** `v1.3-upgraded` 스냅샷 촬영. 삽입 전에 찍으면 H5 재시도마다 다시 넣어야 한다 — H5 는 실패 예상이라 재시도가 실제로 일어난다
   - **값을 그 자리에서 `measurements/metrics.md` 에 적는다.** 업그레이드 적용 시간이 여기서 나온다 — 릴리스마다 고객사에 반복되는 건 최초 설치가 아니라 이쪽이다
10. v1.3 → v1.2 롤백 시도 (이미지 + 스키마) → **H5**
    - 롤백 순서를 먼저 정한다: 앱 중지 → down 적용 → 구버전 기동. 순서가 다르면 결과와 "롤백 소요 시간"이 달라진다
    - 판정은 "서비스가 뜬다"가 아니라 **정규화 후 덤프가 `v1.2-installed` 참조본과 일치**. 컬럼은 지웠지만 인덱스/CHECK 제약이 남아도 앱은 정상 기동하고, 그 상태로 다음 v1.3 재적용이 "index already exists" 로 실패한다
    - 9번에서 삽입한 **v1.3 전용 행**의 유실 여부를 확인한다. 롤백 시 유실 후보는 사실상 이것뿐이다
    - 실패해도 그대로 기록한다. 단계별 3분류 표가 산출물이다
    - **판정과 근거를 `measurements/metrics.md` 와 `docs/findings.md` 양쪽에 적는다.** 전자는 수치, 후자는 원인과 설계 영향
11. (조건부) 빌더 이미지로 VM-2 내부 빌드 → **H8**

### 4단계 — 측정·정리 (2~3일)

12. 지표 수집, 실패 항목 원인 기록, 보고서 작성


## 5. 측정 지표

"됐다/안 됐다"만 적으면 보고서 가치가 낮다. 아래를 반드시 수치로 남긴다.


| 지표 | 측정 방법 | 목표치 |
| --- | --- | --- |
| **패치 1건의 사람 개입 시간** | 번들 생성 지시 → 적용 완료까지 사람이 손 댄 시간만 합산. **운반 시간과 PoC 계측 오버헤드는 따로 적고 합산하지 않는다** | 온라인: 승인 1회 / 폐쇄망: 10분 이내 |
| **PoC 계측 오버헤드** | 스냅샷 촬영·복구, 시계 보정, 양성/음성 대조, 덤프 대조, 증거 반출 | **운영 비용이 아니다.** 합산하면 "10분"이 무조건 실패하고, 빼면 근거가 없다. 별도 기록 |
| **운반 소요 시간** | USB 반입 승인 · 보안 검사 · 담당자 일정까지 포함한 실제 경과 | **측정만 한다.** 위 칸과 합산하지 않되 보고서에 반드시 병기 |
| **번들 크기** | 생성된 tar 크기 | **USB 운반과 고객사 전송 시간을 좌우. 수백 MB 예상** |
| 코어 빌드 시간 | CI 파이프라인 소요 | 참고치 |
| 커스텀 빌드 시간 | base 재사용 시 증분 | 참고치 |
| 번들 적용 시간 | 스크립트 실행 시작 → 헬스체크 통과 | 서비스 중단 시간과 직결 |
| 롤백 소요 시간 | 롤백 지시 → 정상 복귀 | |
| 재실행 안전성 | **스냅샷 복구 없이** 동일 번들 연속 2회 적용 | 멱등해야 함 |
| **계측 양성 대조** | 의도적 외부 연결 1회가 iptables 로그·pcap 양쪽에 잡히는가 | **통과해야 H3 측정이 유효** |
| **외부 통신 시도 횟수** | baseline 초과분. 목적지 (IP, 포트) 유일 조합 + DNS 질의 유일 이름으로 계수 | **0이어야 함** |
| **서명 검증 실패 시 중단** | (a) 페이로드 1바이트 변조 (b) 다른 키로 전체 재서명 | **양쪽 다 중단(exit≠0) + 로그에 `서명 불일치` 기록** |
| 절차 우회 시 설치 성공 여부 | (c) 검증 호출 제거 번들을 `verify-bundle.sh` 없이 실행 | **성공이 기대 결과.** 통제 수단이 대조가 아니라 절차임을 기록 |
| 신규 고객사 1곳 추가 사람 시간 | 매니페스트 작성 → 첫 파이프라인 성공 | H6 의 가치는 사실상 이 숫자다 |
| 저장소 밖 수동 조작 건수 | Harbor 프로젝트·로봇 계정, CI 변수, replication 규칙 등 UI 클릭 | 저장소 수정 0줄이어도 여기가 늘면 의미가 없다 |
| 환경 구축 소요 (a) 사내 1회성 | GitLab·Harbor A/B·VM-1 등 | 고객사 수와 무관 |
| 환경 구축 소요 (b) 고객사 1곳당 | VM-2 상당 환경 + 공개키·검증 스크립트 주입 | **이 값만 고객사 수에 곱한다** |
| 덤프 정규화 자기 대조 | 동일 파이프로 `v1.2-installed` 를 2회 덤프 | **일치해야 H5 판정이 유효** |
| VM-2 스냅샷 촬영·복구 | 스냅샷 ID + 시각 + 복구 후 시계 보정값 + **공개키 지문** | 매 검증 전 필수 |

번들 크기가 예상보다 크면 대응책을 함께 검토한다. base 이미지 슬림화(chiseled/alpine 계열), 레이어 재사용을 통한 증분 번들, 코어 이미지와 커스텀 레이어의 분리 배포 등.

---

## 6. 산출물

1. 동작하는 GitLab CI 템플릿 1벌
2. 번들 생성 스크립트 + 설치 스크립트
3. 측정값 표 (5장 기준)
4. 실패 항목과 원인 목록
5. PoC 결과로 **확정된 ADR**과 **여전히 미결인 ADR** 구분표

5번이 핵심이다. PoC 이후 결정 문서가 어떻게 갱신되는지가 보고서의 결론이 되어야 한다.

---

## 7. 예상 리스크


| 리스크                   | 징후             | 대응                           |
| --------------------- | -------------- | ---------------------------- |
| 폐쇄망에서 숨은 외부 의존성 발견    | VM-2에서 기동 실패   | 실제 망 차단으로 조기 발견. 가장 흔한 실패 지점 |
| 번들 크기가 운반 불가 수준       | tar가 GB 단위     | 슬림 base, 증분 번들, 레이어 분리 검토    |
| 역방향 마이그레이션 불가         | H5 실패          | 순방향 전용 정책 + 스냅샷 복구로 정책 전환 검토 |
| 플러그인 로딩이 코어 수정을 요구    | H2에서 코어를 고치게 됨 | 확장 지점 설계가 PoC 이전에 필요하다는 신호   |
| 운반 중 번들 변조 (폐쇄망)       | 검출 수단 없음       | 체크섬 외 서명 검증 추가, 검증 실패 시 설치 중단 |
| VM-2 상태 오염으로 결과 신뢰 불가  | 재현 안 되는 측정값    | 검증 단계마다 스냅샷 복구 후 시작            |
| **계측이 꺼진 채 H3 통과 (위양성)** | 로그 0건인데 원인 불명 | 양성 대조를 판정 전에 수행. baseline 초과분으로 계수 |
| **빈 DB 로 H4·H5 자동 통과**    | 유실 검사에 비교 대상 없음 | 시드 데이터 선행 적재, 전/후/롤백 후 3회 덤프 대조 |
| 서명 신뢰 앵커가 번들 안에 있음      | 검증은 통과, 변조는 미검출 | 공개키를 대역 외 주입해 `clean` 스냅샷에 포함 |
| v1.2 이미지 부재로 롤백 실행 불가   | 10번에서 멈춤       | v1.0/v1.2/v1.3 번들을 1회에 운반 |
| 4단계가 달력에서 압축됨           | 측정값을 기억으로 채움   | 배분 합이 3주를 초과함을 9장에서 먼저 확정 |
| **검증 코드가 번들 안에서 옴**      | 검증 OK 출력 후 정상 설치 | `verify-bundle.sh` 선행 실행을 **절차로 고정**, `install.sh` 도 서명 대상. 음성 대조 (c)로 우회 가능성을 기록 |
| 내부 트래픽이 시도로 계수됨         | H3 이 항상 비-0     | LOG·pcap 에 사설대역·루프백 제외. 제외 범위도 대조 |
| 덤프 도구 노이즈로 H5 위양성 실패    | "역시 롤백 불가"     | 정규화 파이프 고정 + 자기 대조 |
| 스냅샷 복구가 번들·이미지·증거 삭제   | 재운반, 참조본 소실    | 별도 볼륨 분리 + 복구 전 반출 |
| PoC 성공이 설계 검증으로 과대 해석 | —              | 0장 한계 문구를 보고서 서두에 배치         |


---

## 8. 보고서 목차 (제안)

1. 배경과 PoC 목적
2. **PoC가 답하지 않는 것** (0장 인용)
3. 검증 가설과 결과 요약표
4. 측정값
5. 실패 항목과 원인
6. 설계 결정에 미치는 영향 (확정 / 미결 구분)
7. 다음 단계 권고

---

## 9. 미확정 항목

이 계획은 아래가 정해지면 조정이 필요하다.

- **기간.** 4장 배분 합이 18~20 영업일로 3주를 3~5일 초과한다. (a) 4주로 늘리거나 (b) 축소 1·2·3번을 선반영한 3주 중 하나를 골라야 한다. 결정하지 않으면 4단계가 조용히 사라진다
- **하이퍼바이저 종류.** live snapshot(메모리 포함)인지 디스크 온리인지에 따라 복구 후 시계 보정 필요 여부와 컨테이너 상태가 달라진다. 기록하지 않으면 재현 불가
- **투입 인원.** 1인 3주로는 끝나지 않는다(0단계 2~4일 + 스냅샷·관측 운영 3~5인시 + up 4종·down 4종·시드 2~3일(up 은 1단계, down 은 3단계)). 2인이면 빠듯하게 가능하다. 5장 핵심 지표가 "사람 개입 시간"인데 측정자가 몇 명인지 모르면 목표치의 의미도 흔들린다
- **스냅샷 촬영·복구가 가능한** 모의 폐쇄망 VM 확보 여부 (물리 PC 한 대뿐이면 3장의 상태관리 전체가 실행 불가. 불가 시 H3·H8 검증 불가 → PoC 가치 대폭 하락)
- 전제 문서 `erp-cicd-design-decisions.md` 반입 여부. **3주차 시작 전까지** 필요하다 — 6장 산출물 5번(확정/미결 ADR 구분표)은 이 문서 없이 작성 자체가 불가능하고, 6장은 그것을 "핵심"이라고 못박고 있다. 반입이 안 되면 산출물 5번을 "PoC 결과가 영향을 주는 결정 목록"으로 낮춰 잡아야 한다
- 보고서 대상 독자 (경영진이면 4·6장 중심, 기술팀이면 3·5장 중심)

